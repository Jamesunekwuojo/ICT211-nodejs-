// var numbers = [45, 56, 67, 87];
// for (var x = 0; x<numbers.length; x++) {
//     console.log(numbers[x])
// }

// var numbers = [45, 56, 67, 87];
// for (var x = numbers.length -1; x >=0; x--) {
//     console.log(numbers[x]);
// }

// create a simple array storing set of numbers and determine the even numbers and the odd no in the array
let arr = [23, 34, 56, 67, 45]
for (let i=0; i<arr.length; i++){
    if(arr[i] %2 ==0){
        console.log(`${arr[i]} it's an even number`)
    }
    else{

        console.log(`${arr[i]} is an odd number`)

        
    }
}
